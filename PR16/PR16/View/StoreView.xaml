<Window x:Class="PR16.View.StoreView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PR16.View"
        xmlns:services="clr-namespace:PR16.Services"
        mc:Ignorable="d"
        Title="StoreView" Height="450" Width="800" Loaded="WindowLoaded">
    <Window.Resources>
        <services:PriceColorConverter x:Key="PriceColorConverter"/>
    </Window.Resources>
    <Grid>
        <TabControl Name="MainTabControl">
            <TabItem Header="Товары">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <WrapPanel Grid.Row="0" Margin="10" VerticalAlignment="Center">
                        <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox x:Name="TextBoxSearch" Width="150" Margin="0,0,15,0" TextChanged="TextBoxSearchTextChanged"/>

                        <TextBlock Text="Поставщик:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <ComboBox x:Name="ComboSupplier" Width="150" DisplayMemberPath="Наименование" Margin="0,0,15,0" SelectionChanged="ControlChanged"/>

                        <TextBlock Text="Сортировка (склад):" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <ComboBox x:Name="ComboSort" Width="150" SelectionChanged="ControlChanged">
                            <ComboBoxItem Content="Без сортировки"/>
                            <ComboBoxItem Content="По возрастанию количества"/>
                            <ComboBoxItem Content="По убыванию количества"/>
                        </ComboBox>
                    </WrapPanel>

                    <DataGrid x:Name="DataGridProducts" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Margin="10" CanUserSortColumns="False" MouseDoubleClick="DataGridProductsMouseDoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Код" Binding="{Binding Код}" Width="50"/>
                            <DataGridTextColumn Header="Описание" Binding="{Binding ОписаниеТовара}" Width="*"/>
                            <DataGridTextColumn Header="Кол-во на складе" Binding="{Binding КолВоНаСкладе}" Width="120"/>
                            <DataGridTextColumn Header="Тип товара" Binding="{Binding ТипТовара1.ТипТовара1}" Width="120"/>
                            <DataGridTextColumn Header="Производитель" Binding="{Binding Производитель1.Наименование}" Width="120"/>
                            <DataGridTextColumn Header="Поставщик" Binding="{Binding Поставщик1.Наименование}" Width="120"/>
                            <DataGridTextColumn Header="Цена" Binding="{Binding Цена}" Width="100">
                                <DataGridTextColumn.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Background" Value="{Binding Цена, Converter={StaticResource PriceColorConverter}}"/>
                                    </Style>
                                </DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel x:Name="AdminProductPanel" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                        <Button Content="Добавить" Width="100" Margin="0,0,10,0" Background="LightGreen" Click="ButtonAddProductClick"/>
                        <Button Content="Удалить" Width="100" Background="LightCoral" Click="ButtonDeleteProductClick"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Просмотр заказов">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="DataGridOrders" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True" Margin="10" CanUserSortColumns="False" MouseDoubleClick="DataGridOrdersMouseDoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Номер заказа" Binding="{Binding Код}" Width="100"/>
                            <DataGridTextColumn Header="Дата заказа" Binding="{Binding ДатаЗаказа, StringFormat=\{0:dd.MM.yyyy\}}" Width="130"/>
                            <DataGridTextColumn Header="Клиент (Фамилия)" Binding="{Binding Фамилия}" Width="150"/>
                            <DataGridTextColumn Header="Пункт выдачи" Binding="{Binding КодДляПолучения}" Width="120"/>
                            <DataGridTextColumn Header="Статус" Binding="{Binding СтатусЗаказа1.Наименование}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <StackPanel x:Name="AdminOrderPanel" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                        <Button Content="Добавить заказ" Width="120" Margin="0,0,10,0" Background="LightGreen" Click="ButtonAddOrderClick"/>
                        <Button Content="Удалить заказ" Width="120" Background="LightCoral" Click="ButtonDeleteOrderClick"/>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>

