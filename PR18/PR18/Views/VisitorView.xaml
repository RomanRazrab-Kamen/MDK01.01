<Window x:Class="PR18.Views.VisitorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PR18.Views"
        xmlns:services="clr-namespace:PR18.Services"
        mc:Ignorable="d"
        Title="Виртуальный зоопарк - Режим Посетителя" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <services:BytesToImageConverter x:Key="BytesToImageConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="15">
            <Grid>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Добро пожаловать в Виртуальный Зоопарк!" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>
                </StackPanel>
                <Button Content="Выйти из аккаунта" HorizontalAlignment="Right" VerticalAlignment="Center" Padding="10,5" Background="White" BorderThickness="0" Cursor="Hand" Click="ButtonExit_Click"/>
            </Grid>
        </Border>

        <ListBox Name="AnimalsListBox" 
                 Grid.Row="1" 
                 Margin="10" 
                 Background="Transparent" 
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 MouseDoubleClick="AnimalsListBox_MouseDoubleClick">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" Width="260" Margin="10" >
                        <StackPanel Margin="10">

                            <Button Click="AnimalSound_Click" Tag="{Binding}" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,5">
                                <Border CornerRadius="4" ClipToBounds="True">
                                    <Image Source="{Binding Image, Converter={StaticResource BytesToImageConverter}}" 
                                           Height="160" Width="240" Stretch="UniformToFill"/>
                                </Border>
                            </Button>

                            <TextBlock Text="Описание:" Margin="0,5,0,2"/>
                            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" MaxHeight="60" TextTrimming="CharacterEllipsis"/>

                            <TextBlock Text="Интересный факт:" FontSize="12" FontWeight="SemiBold" Margin="0,8,0,2"/>
                            <TextBlock Text="{Binding InterestingFacts}"/>

                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
